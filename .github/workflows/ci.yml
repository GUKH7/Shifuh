name: CI

on:
  pull_request:
  push:
    branches:
      - main

permissions:
  contents: read
  pull-requests: write

jobs:
  validate:
    runs-on: ubuntu-latest
    timeout-minutes: 20

    env:
      NEXT_PUBLIC_SUPABASE_URL: https://example.supabase.co
      NEXT_PUBLIC_SUPABASE_ANON_KEY: ci-placeholder-anon-key
      SUPABASE_SERVICE_ROLE_KEY: ci-placeholder-service-role-key
      ORDER_TRACKING_SECRET: ci-placeholder-order-tracking-secret-with-32-characters
      WHATSAPP_BOT_API_URL: http://127.0.0.1:3001
      WHATSAPP_BOT_API_TOKEN: ci-placeholder-token

    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm
      - run: npm ci --ignore-scripts
      - run: npm run audit:service-role
      - name: Health test diagnostic
        id: health_test
        continue-on-error: true
        shell: bash
        run: |
          set -o pipefail
          node --test tests/health.test.js 2>&1 | tee health-test-output.txt
      - name: Publish health test diagnostic
        if: ${{ steps.health_test.outcome == 'failure' && github.event_name == 'pull_request' }}
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          {
            echo "## Diagnóstico isolado do health test"
            echo
            echo '```text'
            cat health-test-output.txt
            echo '```'
          } > health-test-comment.md
          gh pr comment "${{ github.event.pull_request.number }}" --body-file health-test-comment.md
      - name: Enforce health test
        if: ${{ steps.health_test.outcome == 'failure' }}
        run: exit 1
